%h1 #{user.name}'s treasures

%ul
  - for item in user.items
    %li{:class => nil}
      #{item.name}, #{item.price} Credits,
      - if item.is_active?
        %a{:href=>"/buy?seller=#{user.name}&product=#{item.name}&buyer=#{name}"}
          (Buy)
      - else
        %i
          (inactive)
%a{:href => "/logout"} Logout